# Copy to k8s/github-mcp-secret.yaml (git-ignored), set a real read-scoped
# GitHub PAT, then: kubectl apply -f k8s/github-mcp-secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: github-mcp-auth
  namespace: kagent
type: Opaque
stringData:
  GITHUB_PERSONAL_ACCESS_TOKEN: "<github-pat>"
